# Local development settings for the OpenGuild backend.
# Load this in your shell (e.g. `source backend/.env`) before running the server.

RUST_LOG=info,openguild_server=debug

# Primary Postgres connection string used by both the runtime and SQLx macros.
DATABASE_URL=postgres://openguild:openguild@localhost:5432/openguild
OPENGUILD_SERVER__DATABASE_URL=postgres://openguild:openguild@localhost:5432/openguild

# Listener configuration.
OPENGUILD_SERVER__HOST=127.0.0.1
OPENGUILD_SERVER__PORT=8080
OPENGUILD_SERVER__SERVER_NAME=dev.openguild.local
OPENGUILD_SERVER__LOG_FORMAT=compact

# Leave metrics disabled by default; uncomment to expose `/metrics` on 9100.
# OPENGUILD_SERVER__METRICS__ENABLED=true
# OPENGUILD_SERVER__METRICS__BIND_ADDR=127.0.0.1:9100

# Optional: provide a stable session signing key (URL-safe base64, 32 raw bytes).
# Generate with: openssl rand -base64 32 | tr '+/' '-_' | tr -d '='
# OPENGUILD_SERVER__SESSION__SIGNING_KEY=
