# Public runtime config consumed via `useRuntimeConfig().public`.
# Copy to `.env` (local) or `.env.development` / `.env.production` as needed.

NUXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8080

# Feature flag stubs; toggle to enable mock data or experimental screens.
NUXT_PUBLIC_ENABLE_MOCKS=false
NUXT_PUBLIC_ENABLE_DEVTOOLS=true
